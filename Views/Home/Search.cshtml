@{
    var articles = ViewBag.articles;
    var pattern = ViewBag.pattern;
}
<div class="page-header">
    <h2>"@pattern"的搜索结果</h2>
</div>
<div class="list-group">
    @foreach (var article in articles)
    {
        <a href="/Home/Article?id=@article.Id" class="list-group-item">
            <h3>@article.Title <small>@article.Author</small></h3>
        </a>
    }
</div>
